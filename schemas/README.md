# JSON Schemas

This folder contains **versioned JSON Schemas** for slide_smith’s public artifacts.

Notes:
- Existing v1 deck spec schema lives in `docs/design/deck-spec.schema.json` (historical).
- v1.2 introduces additional artifacts (StyleProfile, SlidePlan, DeckSpec) for the exemplar-first pipeline.

Files (v1.2, draft):
- `style-profile.schema.json`
- `slide-plan.schema.json`
- `deck-spec-exemplar.schema.json`

These schemas are intentionally minimal at first; they will be tightened as implementations land.
